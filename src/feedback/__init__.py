"""Feedback domain.

Captures free-form product feedback from authenticated teachers (category,
optional 1–5 rating, message) and persists it to the ``feedback`` table for
later review. Mirrors the domain-driven layout of ``src/settings`` and
``src/users`` (``constants``/``models``/``schemas``/``service``/``router``).
"""
