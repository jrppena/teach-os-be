# Lesson-plans domain package.
#
# Holds the provider-agnostic prompt assets used to generate DepEd MATATAG / ILAW
# lesson plans from the teacher-os-fe wizard inputs (see `prompts.py`). The actual
# generation call is made by whichever AI provider the user has marked active in
# Settings (Grok or Gemini); the prompt below is written to work with either.
